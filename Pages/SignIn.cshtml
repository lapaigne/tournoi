@page
@model SignInModel
@{
}

<div id="signin-wrapper">
    <form method="post" hx-post hx-target="#signin-wrapper" hx-boost="true">
        <input name="Token" type="password" placeholder="Введите токен"/>
        <button type="submit">Отправить</button>
    </form>
</div>
